<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.dlis</groupId>
    <artifactId>dlis-modernisation-liberty</artifactId>
    <version>2.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>dlis-application-svc</artifactId>
  <packaging>war</packaging>
  <name>DLIS Application Lifecycle Service (Liberty)</name>

  <dependencies>
    <dependency>
      <groupId>com.dlis</groupId>
      <artifactId>dlis-common</artifactId>
    </dependency>

    <!-- Jakarta EE 10 Web Profile API -->
    <dependency>
      <groupId>jakarta.platform</groupId>
      <artifactId>jakarta.jakartaee-web-api</artifactId>
      <scope>provided</scope>
    </dependency>

    <!-- MicroProfile 6.1 (RestClient, Config, Health, OpenAPI) -->
    <dependency>
      <groupId>org.eclipse.microprofile</groupId>
      <artifactId>microprofile</artifactId>
      <type>pom</type>
      <scope>provided</scope>
    </dependency>

    <!-- DB2 Driver -->
    <dependency>
      <groupId>com.ibm.db2</groupId>
      <artifactId>jcc</artifactId>
      <scope>provided</scope>
    </dependency>

    <!-- Test dependencies -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-junit-jupiter</artifactId>
      <version>${mockito.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>dlis-application-svc</finalName>
    <plugins>
      <plugin>
        <groupId>io.openliberty.tools</groupId>
        <artifactId>liberty-maven-plugin</artifactId>
        <configuration>
          <serverName>applicationServer</serverName>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
